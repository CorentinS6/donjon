{% extends 'ndjDAdminBundle:Default:layout.html.twig' %}

{% block body -%}
    <h1>Objets list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Cat</th>
                <th>Description</th>
                <th>Bonus</th>
                <th>Prix</th>
                <th>Frequence</th>
                <th>Degat</th>
                <th>Bouclier</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('objets_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.cat }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.bonus }}</td>
                <td>{{ entity.prix }}</td>
                <td>{{ entity.frequence }}</td>
                <td>{{ entity.degat }}</td>
                <td>{{ entity.bouclier }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('objets_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('objets_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('objets_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
