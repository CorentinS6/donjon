{% extends 'ndjDAdminBundle:Default:layout.html.twig' %}

{% block body -%}
    <h1>Etage list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Niveau</th>
                <th>Nom</th>
                <th>Taille</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('etage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.niveau }}</td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.taille }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('etage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('etage_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('etage_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
